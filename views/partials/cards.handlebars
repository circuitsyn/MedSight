{{!-- Populate cards page start --}}
<div class="container">
  <div class="row">
          {{!-- Injecting card blocks --}}
            {{#each medsightdata}}
                {{> cards/card-block }}
            {{/each}}  
  </div>
  <a href="/">← Back To Home</a>
</div>
{{!-- Populate cards page end --}}