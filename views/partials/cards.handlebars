<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title">Cards</h5>
          <ul>
            {{#each medsightdata}}
              {{#unless 2}}
                {{> cards/card-block }}
              {{/unless}}
            {{/each}}
          </ul>
          <hr>
          <ul>
          {{#each medsightdata}}
            {{#if 2}}
              {{> cards/card-block }}
            {{/if}}
          {{/each}}
        </ul>                 
            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>
